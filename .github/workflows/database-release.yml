name: Release

on:
  push:
    branches:
      - main
    paths:
      - 'packages/database/**'
      - '.github/workflows/database-release.yml'

jobs:
  publish:
    runs-on: ubuntu-latest

    permissions:
      contents: write
      issues: write
      pull-requests: write
      id-token: write
      packages: write

    steps:
      - name: Checkout code
        uses: actions/checkout@v2
        with:
          fetch-depth: 0
          path: jsx-mail

      - name: Install bun
        uses: oven-sh/setup-bun@v1

      - name: Set up Node.js
        uses: actions/setup-node@v2
        with:
          node-version: 'lts/*'

      - name: Prepare and release
        env:
          ENV_FILE: ${{ secrets.ENV_FILE }}
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          cp -rf jsx-mail/packages/database database
          cp -rf jsx-mail/.git database/.git
          cd database
          echo "//npm.pkg.github.com/:_authToken=$GITHUB_TOKEN" >> .npmrc
          echo "@jsx-mail:registry=https://npm.pkg.github.com" >> .npmrc
          echo "$ENV_FILE" > .env
          yarn install
          yarn run build
          yarn run prepare:db
          yarn release
          cd ../../

          git config user.name github-actions
          git config user.email 41898282+github-actions[bot]@users.noreply.github.com
          git add .
          git commit -m "chore: release database"
          git push
