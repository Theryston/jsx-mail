{
  "name": "@jsx-mail/database",
  "main": "./index.ts",
  "types": "./index.ts",
  "version": "0.0.0",
  "scripts": {
    "prepare:db": "prisma db push && prisma generate && prisma db seed",
    "postinstall": "bun run prepare:db"
  },
  "dependencies": {
    "@prisma/client": "^5.9.1"
  },
  "devDependencies": {
    "prisma": "^5.9.0"
  },
  "prisma": {
    "seed": "bun run seed/index.ts"
  }
}
